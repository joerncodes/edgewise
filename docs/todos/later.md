# Later

Bigger ideas, only if the simple app stops being enough.

- [ ] Alternative `Storage` implementation backed by SQLite — would let the
      app live on a Vercel-style platform with persistent volumes.
- [ ] Multi-user mode. Currently single password. Auth.js makes this
      cheap if needed.
- [ ] Stones / equipment as separate entities, linked from sessions.
- [ ] Invoicing — track per-knife cost and produce a per-owner statement.
- [ ] Mobile-friendly capture flow for adding a session from the bench.
